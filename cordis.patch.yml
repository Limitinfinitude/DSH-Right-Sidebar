# Output Dock bundle patch: mounts the explicit publication tool and its client.
# The node half validates published results and serves approved workspace files;
# the browser half renders per-session, user-opened result tabs.
- insert:
    - id: output-dock
      name: dsh-output-dock
